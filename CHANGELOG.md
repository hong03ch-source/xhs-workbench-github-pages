# 更新说明

这一版把之前评审出来的问题全部改掉了，并按「主界面更清爽」重做了首屏。

---

## 一、会丢数据 / 有风险的（最要紧）

**启动时会覆盖云端数据的 bug。**
旧版 `bootstrap()` 最后一行无条件 `Sync.save(state)`。只要云端读取失败（网络抖动、
令牌过期、GitHub 限流），本机的空数据就会被推上去，静默覆盖掉云端正确的数据，
不可恢复。现在只有确认「本次数据确实来自云端」才解锁云端写入，否则进入只读模式，
顶部挂提示条，由你决定重试还是强制以本机为准。

**没有冲突检测。**
旧版整体覆盖，`state.json` 里写了 `updated_at` 却从来不读。电脑和手机同时开着，
后保存的会把前一个全量吃掉。现在推送前先比对云端时间戳，不一致就中止并提示，
让你选「拉取云端」还是「以本机为准」。

**Service Worker 让更新永远生效不了。**
旧版对所有同源请求 cache-first，缓存名写死 `xhs-wb-v1`。你 push 了新代码，
老设备永远加载旧版本。现在 HTML/JS/CSS 走 network-first（离线仍能打开），
其余走 stale-while-revalidate，发现新版本会弹「有新版本 · 刷新」。

**令牌明文永久存储且无法清除。**
现在可以选「只记住本次会话」（存 sessionStorage，关掉浏览器即失效），
并提供一键清除令牌。弹窗里也写清了令牌是账号级凭证这件事，
并给出了完整的取令牌步骤 —— 特别标注了**必须用 classic token**，
GitHub 的 fine-grained 令牌目前不支持 Gist，填了会一直连不上。

**推送太频繁。**
旧版每次 `save()` 都直接 PATCH 一次 Gist，在输入框里打字会打满请求。
现在 1.5 秒防抖 + 合并，切后台时立即把没推完的推出去。

---

## 二、每天都会碰到的

- **手机上看板不能用。** HTML5 拖拽在 iOS Safari 根本不触发 touch，而旧版是
  6 列 × 274px 横向滚动。现在窄屏改成单列 + 顶部阶段切换，卡片上有
  「← 上一阶段 / 下一阶段 →」按钮。
- **手机上 KPI 全消失。** 旧版 `.stat-strip` 在 880px 以下直接 `display:none`。
  现在状态条在手机上照常显示。
- **「本周发布节奏」统计的其实是「本周复盘了几条」。** 旧版用的是复盘提交日期。
  现在给选题加了 `publishedAt`（拖进「已发布」自动打时间戳，带小时），
  用它统计，也用它算「什么时段发效果最好」。
- **复盘记录不能编辑删除。** 填错一次就永久污染平均值和作品集排序。现在可删，可撤销。
- **没有 URL hash。** 刷新回工作台、后退直接退出 PWA。现在每个视图有独立地址。
- **删除用的是 `confirm()`。** 现在全部改成可撤销的 toast，误删有后悔药。

---

## 三、逻辑与数据模型

- **一条视频只能复盘一次** → 现在可以多次采集。发布 24 小时和 7 天是两回事，
  现在能看出「慢热型」还是「首日型」，慢热的那类最值得再做一条同款。
- **诊断阈值写死**（留存 40% / 完播 15% / 赞藏 5% / 转粉 0.5%）→ 冷启动仍用这套，
  但攒够 5 条后自动改成跟你自己近 10 条的中位数比。「低于你平时」比「低于行业」有用。
- **粉丝数只有当前值，每次覆盖** → 改成历史记录，能算日均涨粉和「按这个速度还要
  多少天到门槛」。
- **标签是单值自由文本** → 改成多标签，复盘页会按标签聚合出转粉率对比。
- **「蒲公英已入驻」靠 `粉丝≥1000 && 有商单` 反推** → 改成手动勾选，这本来就不是能推断的事。
- **新增横向洞察**：哪类钩子留得住人、哪类内容更能转粉、什么时段完播更好。
  样本不够时明说还差几条，不给假装可信的结论。
- **爆款没有出口** → 加了「复制一条」，好选题可以换角度重做。
- **新增日历视图**，回答「这个月发了什么、下周是不是空的」。
- **新增素材 / 成片链接字段**，复盘时不用再回 App 里翻。
- **`startFans` 字段名和用途不符**（实际是当前粉丝）→ 改成 `fansNow`，旧数据自动迁移。

---

## 四、内容与合规

- **热点雷达说「每天 9:00 自动抓取抖音 / 小红书」，但仓库里从来没有定时任务。**
  现在改成两个来源并存的**热点库**：
  - **你自己记的** —— 随手存，一键变成选题。`suggestedTitle` 独立成字段
    （旧版靠 `split("》——")` 猜标题，换条数据就会把整段当标题）。
  - **每周搜索聚合** —— 见下面「新增：自动热点聚合」。
- **合规检查漏掉真正的风险。** 旧版只查 12 个海外品牌名。实际上小红书更常见的
  限流原因是绝对化用语（最、第一、100%、必看）和诱导互动。现在三类都查，
  并支持**一键替换**（旧版只提示不动手）。
- **对标博主写死在代码里、链接是搜索结果页、数据没有时间戳。**
  现在升级成**爆款拆解库**：可增删改，链接建议存主页，每条带记录日期
  （「2.2 万赞」是某天的快照，三个月后会误导你）。
- **报价表和门槛数字硬编码在三四个文件里** → 全部集中到 `assets/config.js`，
  报价表带「记录于 2026-07」，门槛旁边提示以官方公告为准。

---

## 五、主界面精简

- **删掉 header 那 5 个数字胶囊。** 它们全是冗余：粉丝数在状态条、已发布口径和
  状态条打架、待复盘在聚焦句里、制作中等于中间两列相加、蒲公英状态商单页有完整版。
- **看板 6 列压到 4 列。** 「复盘」不是生产阶段而是已发布视频的属性，并入「已发布」
  用标记区分；「拍摄」「剪辑」对单人博主常是同一天的事，合成「制作中」加细分标签。
  4 列在 1180px 容器内一屏放得下，不用再横向滚动。
- **三张仪表卡（约 140px）压成一行状态条（44px）**，看板直接上移进首屏。
  「今日聚焦」只留优先级最高的那一条 —— 旧版会同时列出 5 条以上，那不叫聚焦。
- **卡片瘦身**：从最多 8 个视觉元素降到 3–4 个，编辑/删除改成 hover 才出现。
- **去掉视觉噪音**：5 处红色发光只留主按钮、去掉两层背景渐变、去掉卡片顶部高光线、
  hover 不再位移（列表里位移最晃眼）。
- **字号从 9 档带半像素收敛成 5 档整数**（11/12/13/15/20）。
- **新增深色模式**，跟随系统，右上角可手动切换。

---

## 六、工程

- **无障碍**：卡片可 focus 并用方向键移动阶段、导航是真正的 tablist、
  弹窗有焦点陷阱和焦点归位、Esc 能关掉任意弹窗（旧版只关得掉选题弹窗）。
- **XSS 面**：旧版全站字符串拼 innerHTML，`esc()` 还漏了单引号。
  现在所有用户内容走 `textContent`，从根上不存在这个问题。
- **只重渲染受影响的列**，不再拖一次卡片整个看板重建。
- **manifest**：解除竖屏锁定（iPad 横屏被强制转竖）、补 maskable 图标、
  加长按图标快捷方式、补 description 和 OG 标签。
- **自检模式**：访问 `?selftest=1` 会跑一遍日期、聚合、诊断、合规、数据迁移的断言。
  这几个纯函数算错是静默的 —— 你不会发现，但会照着错的数字做决定。
- **页脚显示版本号**，排查「我手机上是不是旧版本」时省事。

---

---

## 启动速度优化（三端「点开就在」）

**改掉了三个拖慢启动的设计，其中两个是我自己上一版引入的。**

**1. 不再等云端才画界面。**
旧的启动是 `await Sync.load()` —— 必须先跟 GitHub 完成一次网络往返，才肯画第一帧。
手机信号差的时候就是两三秒白屏。

现在改成本地优先：本地数据同步读出来立刻渲染（几十毫秒），云端在后台对账，
回来之后再决定要不要换。写云端的安全约束没有放松 —— 云端确认可读之前
依旧不允许写，这段时间的改动只落本地。

对账时还多做了一层判断：如果你在云端回来之前已经改过东西，**不会**默默用云端覆盖，
而是挂一条横幅让你自己选「用云端的」还是「保留本机的」。

**2. Service Worker 改回 stale-while-revalidate。**
上一版我为了让更新能生效，把 HTML/JS/CSS 改成了 network-first —— 结果每次打开
都要先等网络，恰恰把「秒开」牺牲掉了。现在有缓存就直接给缓存，后台再拉新的；
更新走 SW 自己的版本机制，页面启动时主动调一次 `registration.update()` 兜底，
发现新版本弹「有新版本 · 刷新」。

> 注意：以后改代码，记得把 `sw.js` 顶部的 `VERSION` 改掉，否则老设备不会更新。

**3. 只渲染看得见的那个视图。**
旧版每次刷新都把 8 个模块全渲一遍（含日历的 42 个格子），其中 7 个是隐藏的。
现在只渲染当前视图，每次操作的渲染量大约是原来的六分之一。
热点库那个 `hotspots.json` 也改成了进入热点页时才去取，不再抢首屏。

**4. 干掉深色模式的白屏闪烁。**
主题原来是 JS 加载完才应用的，手动选了深色的人每次打开都会先闪一下白。
现在主题单独存一个极小的 key，`<head>` 里一段内联脚本在第一帧之前就设好。

**5. 预连接 api.github.com**，第一次同步少等一个 TLS 握手。

---

## iPhone / iPad 上的手感

这几条不影响速度，但决定「像 App 还是像网页」：

- 点按不再出现灰色高亮方块（`-webkit-tap-highlight-color`）
- 长按卡片、标签、按钮不再选中文字弹复制菜单（输入框和正文正常可选）
- 去掉点击延迟（`touch-action: manipulation`）
- 整页橡皮筋回弹关掉（`overscroll-behavior-y: contain`），装到主屏幕后更像原生
- 输入框在 iOS 上强制 16px —— 小于这个值 Safari 聚焦时会强行放大整个页面
- 切视图的淡入从 200ms 压到 120ms，再长就「看得见在等」

---

## 新增：扫码配对（三端同步不用重复申请令牌）

在手机上重新申请一次 GitHub 令牌要走十来步，还得用手机键盘敲一长串 `ghp_`。
现在：**数据与同步 → 添加另一台设备 → 手机相机扫一下 → 完成。**

凭证走的是「屏幕 → 摄像头」，不经过网络，也不经过任何第三方 App。

安全上做了四件事：

1. 配对信息放在 URL 的 `#` 片段里 —— 浏览器不会把 `#` 后面的内容发给服务器
2. 另一台设备一打开就立刻 `history.replaceState` 清掉地址栏，不留在浏览历史
3. 内置 10 分钟有效期，万一被截图了，过期也用不了
4. 弹窗关闭时把链接从 DOM 里擦掉，二维码画布也清空

二维码组件从 CDN 按需加载；没网加载不出来时自动降级成一个可复制的链接。

**注意：** 这个二维码等同于你的令牌，当面扫，别截图发人。

---

## 关于令牌：一处之前写错的说明

旧说明里写「建议用 fine-grained token」——**这是错的**。
GitHub 的 fine-grained 令牌目前不支持 Gist，填了会一直连不上。
必须用 **Tokens (classic)**，权限只勾 `gist` 一个。
完整步骤现在直接写在同步弹窗里了。

---

## 新增：每周热点聚合

**机制（当前采用）。** Cowork 里建了一个每周一早上 9:00 的定时任务 `weekly-hotspots`。
它用网络搜索找**公开网络已索引的内容**（行业报道、第三方数据平台的公开榜单页、
周报月报、被搜索引擎收录的讨论），筛选后直接写进 `data/hotspots.json`，并把文件给你过目。

不访问抖音 / 小红书的任何接口，不绕过登录，不抓受限内容。搜的是搜索引擎天天在抓的东西。

**不需要 API key，不需要绑卡，不额外花钱。**
唯一需要你做的：任务跑完之后把改动 push 到 GitHub，线上才会更新。

**它只在 Claude 桌面端开着的时候跑。** 周一没开电脑的话，下次打开会补跑。

**备用方案（默认关闭）。** `scripts/fetch-hotspots.mjs` 和
`.github/workflows/hotspots.yml` 留在仓库里，走的是 Claude API + GitHub Actions
全自动的路子——连 push 都是机器人做，你完全不用管。默认关掉了定时触发，
不会在 Actions 页面留一堆空记录。哪天你不想每周手动 push 了，就：

1. 仓库 → Settings → Secrets and variables → Actions，加一个名为
   `ANTHROPIC_API_KEY` 的 secret（注意：Claude 订阅不含 API 额度，
   要单独在 console.anthropic.com 开开发者账号并充值）
2. 把工作流里 `schedule` 那两行取消注释

成本约每月 0.2 美元（web_search 每 1000 次搜索 $10，一周只用 6 次）。

**为什么是周更不是日更。** 搜索引擎的索引有延迟，「当天的爆款」基本搜不到，
能搜到的是榜单页和行业总结。日更的实际结果是：同样的关键词、同样的索引，
你会连着七天看到几乎一样的卡片——重复会让你很快不再相信这个模块。
方向级的信息一周变一次差不多。

**为什么每条都强制带来源链接。** 搜回来的东西里一定混着营销号，
AI 归纳完之后看起来和真榜单一模一样。脚本里加了一道硬校验：
**没有合法来源链接的条目直接丢掉**，宁可这周只出 2 条，也不放一条看起来很像真的假热点。
前端每张卡片下面都把来源列出来，点开就能自己验。

**为什么建议区和你的库分开。** 搜来的先当「建议」展示，点「采用」才写进你的库。
这样你的热点库里始终只有你亲自认可过的东西，统计和引用都不会被污染。

**期望值。** 这类搜索拿回来的多半是「这个方向在火」，不是「这条视频爆了」。
方向级输入对定选题有用，但别指望它给你能直接抄的爆款清单。

---

## 还剩一件没做

**PNG 图标。** manifest 里目前只有 SVG。Chrome on Android 对 SVG 应用图标支持不好，
装到安卓桌面可能显示异常（iOS 和桌面端正常）。补法：用 `assets/icon.svg` 导出
192×192 和 512×512 的 PNG 放进 `assets/`，然后在 `manifest.webmanifest` 的
`icons` 数组里各加一项。

---

## 文件变化

| 文件 | 状态 |
|---|---|
| `assets/config.js` | 新增 · 所有阈值/门槛/词库集中在这里 |
| `assets/data.js` | 重写 · 只放示例数据，不再默认混入真实数据 |
| `assets/sync.js` | 重写 · 安全启动、冲突检测、凭证管理 |
| `assets/app.js` | 重写 |
| `assets/styles.css` | 重写 · 精简 + 深色模式 |
| `index.html` | 重写 |
| `sw.js` | 重写 · network-first |
| `manifest.webmanifest` | 重写 |
| `assets/icon-maskable.svg` | 新增 |
| `data/hotspots.json` | 新增 · 每周聚合结果，由 Actions 自动更新 |
| `scripts/fetch-hotspots.mjs` | 新增 · 聚合脚本 |
| `.github/workflows/hotspots.yml` | 新增 · 每周一 09:00 定时任务 |
| `data/hotspots.js` | 废弃 · 留空壳防止旧缓存 404，确认各设备都更新后可删 |
| `vercel.json` | 未动 |

首次打开会问你「加载示例 / 从空白开始」。选空白就是干净的。
